# `en2m` on the host (S3 coordinator)

This copy of `en2m` is for building **`firmware/coordinator`** (mesh + USB path).

Cluster / driver APIs for end devices live in the **device** repo:

https://github.com/SFNFIH/espnow2mqtt-device/tree/main/components/en2m
